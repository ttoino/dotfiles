{
  wayland.windowManager.hyprland = {
    enable = true;
    
    settings = ({
    })
    // (import ./apps.nix)
    // (import ./catppuccin.nix)
    // (import ./keybinds.nix)
    // (import ./options.nix)
    // (import ./rules.nix);
  };
}
